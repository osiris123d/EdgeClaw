/**
 * Logical shared-workspace project id for **debug orchestration** and the local
 * `npm run sandbox:orchestration` harness only — not customer data.
 *
 * TODO(remove): If this debug surface is removed, keep the id documented only in fixtures
 * or migrate sandbox harness to a new constant.
 */
export const ORCHESTRATION_DEBUG_SHARED_PROJECT_ID = "cf-truth-fixture-sandbox-orchestration-micro";
